# Session Summary: 2026-04-20
Project: Color Crush Candy (D:\Projects\candy)

## What Shipped

### 1. Category Taxonomy (migration 0004)
4-dimension hierarchy via `categories.parentId` self-reference:
- Shop by Type / Shop by Color / Shop by Event / Gift Boxes
- Remap-not-wipe migration kept existing `product_categories` links intact
- Storefront header: 4 dropdown menus (DB-driven)
- Home page tiles + event chips: DB-driven
- Admin: parent picker + tree view on `/admin/categories`; grouped checkboxes on product edit form
- New endpoints: `GET /api/categories?tree=true`, `/api/products` root-slug expansion

### 2. Trending Now (home page)
- `sort=trending` in `/api/products` — orders by `page_views` count (last 30 days), backfills alphabetically to 8 products

### 3. Category Image Overlays (home page)

### 4. Maintenance-Mode YouTube Intro Video
- Component: `src/components/MaintenanceVideo.tsx`
- YT IFrame API, autoplay muted, overlay blocks controls, mute toggle, fades to static hero on ENDED
- Admin settings: toggle enable/disable + URL input

### 5. Admin Announcement Bar
- Configurable in admin settings: enable/disable + text field
- Supports `{freeShippingThreshold}` token

### 6. Scheduled Alerts System (migrations 0005 + 0006)
- Table: `scheduled_alerts` (type: date | inventory, severity, notifiedAt)
- Admin page: `/admin/alerts` with holiday presets + lead-days calculator
- Sidebar badge polls `/api/alerts/active` every 60 s
- Dashboard KPI card

### 7. Cron Notification Endpoint
- `POST /api/cron/alerts` — Bearer `CRON_SECRET` auth
- Emails all admin + super_admin users when alerts fire
- Sets `notifiedAt` on fire; un-acknowledging clears it to allow re-notification

### 8. Migration Runner Overhaul
- `scripts/migrate.ts` uses `drizzle-orm/neon-http/migrator` (replaces broken `drizzle-kit migrate`)
- `npm run migrate` and `npm run migrate:status`
- Shared helper: `src/lib/db/migration-status.ts`
- Runtime status API: `/api/migrations/status`
- Amber `<MigrationBanner>` shown across admin when DB is behind schema

### 9. Production Build Fixes
- Switched build to `next build --webpack` (turbopack standalone chunk-emit bug in Next 16)
- Disabled in-build TS + ESLint passes (prevented OOM); added `npm run typecheck` as separate script
- `NODE_OPTIONS=--max-old-space-size=4096` in deploy.sh

### 10. Deploy Script (`scripts/deploy.sh`)
Sequence: git pull → npm ci → pm2 stop → rm -rf .next → build → copy public/ + .next/static/ into standalone → `npm run migrate` → pm2 restart

## Resolved Production Incidents
- Migrations 0002/0003/0004 silently skipped: `drizzle-kit migrate` is a no-op on Neon HTTP. Fixed by applying via psql + inserting tracking rows; permanent fix is new migrate script.
- ChunkLoadError on `/about` etc.: turbopack standalone bug. Fixed via `--webpack`.
- TypeScript OOM during build: fixed by disabling in-build TS + 4 GB heap flag.

## Current State
- All 10 items above are in production (or ready to deploy with `./scripts/deploy.sh` on `/var/www/colorcrush`)
- Modified files not yet committed: `package-lock.json`, `src/app/admin/settings/page.tsx`, `src/app/api/settings/route.ts`, `src/app/globals.css`, `src/app/layout.tsx`, `src/lib/settings.ts`

## Remaining / Not Started
1. Wire the alerts cron on the production host:
   - Generate `CRON_SECRET`, add to `/var/www/colorcrush/.env.local`
   - Add crontab entry (see `docs/cron-alerts.md` for exact command)
2. Optional: add a swap file on the production host if build OOMs persist despite 4 GB flag
3. The uncommitted file changes above should be reviewed + committed
